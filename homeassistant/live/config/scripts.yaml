energyhub_start_panic:
  alias: EnergyHub - Start Panic
  description: Starts manual Panic charging when Autopilot is enabled.
  mode: single
  sequence:
  - choose:
    - conditions:
      - condition: state
        entity_id: input_boolean.energyhub_autopilot
        state: 'on'
      sequence:
      - action: mqtt.publish
        data:
          topic: energyhub/input/ha/inverter_mode
          payload: panic
          retain: false
    default:
    - action: persistent_notification.create
      data:
        title: 'EnergyHub: Panic not started'
        notification_id: energyhub_manual_panic_blocked
        message: >-
          Manual Panic was not started because Autopilot is disabled.

          Enable EnergyHub Autopilot and press Start Panic again.